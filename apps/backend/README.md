# backend

API và nghiệp vụ. Report 2 đề xuất NestJS; framework, auth, database migration chưa chốt. Quyền Zone và trạng thái nghiệp vụ thuộc Backend.

Chưa có code ứng dụng. PR khởi tạo phần này cần thêm cấu hình mẫu, lệnh chạy/build/test, lockfile của package manager đã chọn và CI tương ứng.
